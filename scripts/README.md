# Investigation Scripts
Custom helper scripts used during the lab:
- `sed_anonimizacao.sh`: Automates the masking of IPs and usernames in system logs.
- `tcpdump_command.txt`: The specific syntax used for the initial capture.
